---
---
<div class="nav-glow-wrap"><div class="ng-glow"></div><div class="ng-line"></div><div class="ng-line"></div><div class="ng-line"></div><div class="ng-line"></div><div class="ng-line"></div></div>
<style>
  .nav-glow-wrap {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 90px;
    z-index: 49999;
    pointer-events: none;
    overflow: hidden;
  }
  .nav-glow-wrap .ng-line {
    position: absolute;
    left: 0;
    width: 100%;
    height: 1.5px;
    background: linear-gradient(90deg, transparent 5%, rgba(255,215,60,0.55) 15%, rgba(255,225,100,0.9) 50%, rgba(255,215,60,0.55) 85%, transparent 95%);
  }
  .nav-glow-wrap .ng-line:nth-child(1) {
    top: 10px;
    height: 2px;
    background: linear-gradient(90deg, transparent 8%, rgba(255,215,60,0.5) 20%, rgba(255,225,100,0.85) 50%, rgba(255,215,60,0.5) 80%, transparent 92%);
  }
  .nav-glow-wrap .ng-line:nth-child(2) {
    top: 28px;
    height: 1.5px;
    background: linear-gradient(90deg, transparent 12%, rgba(255,220,80,0.4) 30%, rgba(255,225,90,0.75) 50%, rgba(255,220,80,0.4) 70%, transparent 88%);
  }
  .nav-glow-wrap .ng-line:nth-child(3) {
    top: 46px;
    height: 1.2px;
    background: linear-gradient(90deg, transparent 18%, rgba(255,220,80,0.3) 36%, rgba(255,230,110,0.6) 50%, rgba(255,220,80,0.3) 64%, transparent 82%);
  }
  .nav-glow-wrap .ng-line:nth-child(4) {
    top: 60px;
    height: 1px;
    background: linear-gradient(90deg, transparent 22%, rgba(201,168,76,0.2) 40%, rgba(255,220,80,0.4) 50%, rgba(201,168,76,0.2) 60%, transparent 78%);
  }
  .nav-glow-wrap .ng-line:nth-child(5) {
    top: 74px;
    height: 0.7px;
    background: linear-gradient(90deg, transparent 30%, rgba(201,168,76,0.12) 46%, rgba(255,215,60,0.25) 50%, rgba(201,168,76,0.12) 54%, transparent 70%);
  }
  .nav-glow-wrap .ng-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(ellipse 80% 60% at 50% 0%, rgba(201,168,76,0.04) 0%, transparent 80%);
    pointer-events: none;
  }
  @media (max-width: 767px) {
    .nav-glow-wrap { height: 60px; }
    .nav-glow-wrap .ng-line:nth-child(1) { top: 6px; height: 1.5px; }
    .nav-glow-wrap .ng-line:nth-child(2) { top: 20px; }
    .nav-glow-wrap .ng-line:nth-child(3) { top: 34px; }
    .nav-glow-wrap .ng-line:nth-child(4) { top: 46px; height: 0.8px; }
    .nav-glow-wrap .ng-line:nth-child(5) { top: 54px; height: 0.5px; }
  }
</style>
